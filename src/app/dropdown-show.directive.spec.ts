import { DropdownShowDirective } from './dropdown-show.directive';

describe('DropdownShowDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownShowDirective();
    expect(directive).toBeTruthy();
  });
});
